export enum MessageType {
    ERROR,
    SUCCESS,
    WARNING
}

export enum Key {
    ENTER = "Enter",
    BRACKET_RIGHT = "{",
    SQUARE_BRACKET_RIGHT = "[",
    PARENTESIS_RIGHT = "(",
    TAB = "Tab",
    BACKQUOTE = "'",
    DOUBLE_BACKQUOTE = "\""
}

export enum FileType {
    FIRMWARE,
    AGENT
}

export enum AppEvent {
    MESSAGE = "message"
}

